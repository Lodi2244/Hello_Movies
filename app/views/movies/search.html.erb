<body>

<div class="container">
    <h1>Choose your movie </h1>

<%= form_tag("/movies/search", method: "post") do %>
  <input type="<%= text_field_tag :word %>">
  <br><br>
    <button type="submit" class="btn btn-dark">Search</button>
<% end %>

<div class="card">
  <div class="card-body">
<% if @movies %>
  <% @movies.each do |f| %>
    	 <h5 class="card-title">Title : <%= f.title %></h5>
        <p class="card-text">Director : <%= f.director %></p>
        <i class="card-text">Release date : <%= f.realease_date %></i>
  <% end %>
  <% end %>
</div>
</div>
  </div>

</body>
